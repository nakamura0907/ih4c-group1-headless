query FetchSpotById($id: ID!) {
  spot(id: $id) {
    data {
      attributes {
        name
        description
        photo {
          data {
            attributes {
              url
            }
          }
        }
      }
    }
  }
}
