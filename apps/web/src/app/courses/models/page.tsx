import { Page } from "@/components/page/courses/models";
import { routes } from "@/config";

export const metadata = {
  title: routes.courses.models.index.name,
};

export default function CoursesModelsPage() {
  return <Page />;
}
