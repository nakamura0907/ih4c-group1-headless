import { Page } from "./_page";
import { routes } from "@/config";

export const metadata = {
  title: routes.courses.originals.create.name,
};

export default function CoursesOriginalsCreate() {
  return <Page />;
}
