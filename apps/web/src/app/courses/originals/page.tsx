import { Page } from "@/components/page/courses/originals";
import { routes } from "@/config";

export const metadata = {
  title: routes.courses.originals.index.name,
};

export default function CoursesOriginalsPage() {
  return <Page />;
}
